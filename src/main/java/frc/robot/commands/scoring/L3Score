// Copyright (c) FIRST and other WPILib contributors.
// Open Source Software; you can modify and/or share it under the terms of
// the WPILib BSD license file in the root directory of this project.

package frc.robot.commands.autonomous;

import com.pathplanner.lib.auto.AutoBuilder;
import edu.wpi.first.wpilibj.DriverStation;
import edu.wpi.first.wpilibj2.command.Commands;
import src.main.java.frc.robot.subsystems.elevator.ElevatorSubsystem;
import src.main.java.frc.robot.subsystems.elevator.ElevatorConstants;
import src.main.java.frc.robot.subsystems.arm.ArmSubsystem;
import src.main.java.frc.robot.subsystems.arm.ArmConstants;

public class L3Score extends SequentialCommandGroup {
  /**
   * Creates a new ComplexAuto.
   *
   * @param elevator The elevator subsystem this command will run on
   */
  public L3Score(ElevatorSubsystem elevator, ArmSubsystem arm) {
    addCommands(
 
         new elevator.setHeight(ElevatorConstants.L3_ELEVATION),

         new elevator.setHeight(ElevatorConstants.STARTING_POSITION),
         
         new arm.setAngle(Ar) 
        );
        );
  }

}
